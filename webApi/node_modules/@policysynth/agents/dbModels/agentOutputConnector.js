import { DataTypes, Model } from "sequelize";
import { sequelize } from "./sequelize.js";
export class AgentOutputConnectors extends Model {
}
AgentOutputConnectors.init({
    agent_id: {
        type: DataTypes.INTEGER,
        primaryKey: true,
        allowNull: false,
        references: {
            model: "ps_agents",
            key: "id",
        },
        onDelete: "CASCADE",
    },
    connector_id: {
        type: DataTypes.INTEGER,
        primaryKey: true,
        allowNull: false,
        references: {
            model: "ps_agent_connectors",
            key: "id",
        },
        onDelete: "CASCADE",
    },
}, {
    sequelize,
    tableName: "AgentOutputConnectors",
    timestamps: false,
    underscored: true,
    indexes: [
        { fields: ["agent_id"] },
        { fields: ["connector_id"] },
    ],
});
//# sourceMappingURL=agentOutputConnector.js.map