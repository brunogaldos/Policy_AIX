import { DataTypes, Model } from "sequelize";
import { sequelize } from "./sequelize.js";
export class AgentRegistryConnectors extends Model {
}
AgentRegistryConnectors.init({
    id: {
        type: DataTypes.INTEGER,
        autoIncrement: true,
        primaryKey: true,
    },
    ps_agent_registry_id: {
        type: DataTypes.INTEGER,
        allowNull: false,
        references: {
            model: "ps_agent_registries", // Table name
            key: "id",
        },
        onDelete: "CASCADE", // Matches migration
    },
    ps_agent_connector_class_id: {
        type: DataTypes.INTEGER,
        allowNull: false,
        references: {
            model: "ps_agent_connector_classes", // Table name, assumed from migration
            key: "id",
        },
        onDelete: "CASCADE", // Matches migration
    },
    created_at: {
        type: DataTypes.DATE,
        allowNull: false,
        defaultValue: DataTypes.NOW,
    },
    updated_at: {
        type: DataTypes.DATE,
        allowNull: false,
        defaultValue: DataTypes.NOW,
    },
}, {
    sequelize,
    tableName: "AgentRegistryConnectors",
    timestamps: true,
    underscored: true,
    indexes: [
        {
            fields: ["ps_agent_registry_id"],
        },
        {
            fields: ["ps_agent_connector_class_id"],
        },
    ],
});
//# sourceMappingURL=agentRegistryConnector.js.map