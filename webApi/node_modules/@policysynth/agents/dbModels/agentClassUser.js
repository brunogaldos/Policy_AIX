import { DataTypes, Model } from "sequelize";
import { sequelize } from "./sequelize.js";
export class AgentClassUsers extends Model {
}
AgentClassUsers.init({
    agent_class_id: {
        type: DataTypes.INTEGER,
        primaryKey: true, // Part of a composite primary key
        allowNull: false,
        references: {
            model: "ps_agent_classes",
            key: "id",
        },
        onDelete: "CASCADE",
    },
    user_id: {
        type: DataTypes.INTEGER,
        primaryKey: true, // Part of a composite primary key
        allowNull: false,
        references: {
            model: "users",
            key: "id",
        },
        onDelete: "CASCADE",
    },
}, {
    sequelize,
    tableName: "AgentClassUsers",
    timestamps: false, // As per migration
    underscored: true,
    indexes: [{ fields: ["agent_class_id"] }, { fields: ["user_id"] }],
});
//# sourceMappingURL=agentClassUser.js.map