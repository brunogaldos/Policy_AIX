import { DataTypes, Model } from "sequelize";
import { sequelize } from "./sequelize.js";
export class AgentModels extends Model // Creation attributes are the same
 {
}
AgentModels.init({
    agent_id: {
        type: DataTypes.INTEGER,
        primaryKey: true, // Part of a composite primary key
        allowNull: false,
        references: {
            model: "ps_agents",
            key: "id",
        },
        onDelete: "CASCADE",
    },
    ai_model_id: {
        type: DataTypes.INTEGER,
        primaryKey: true, // Part of a composite primary key
        allowNull: false,
        references: {
            model: "ps_ai_models",
            key: "id",
        },
        onDelete: "CASCADE",
    },
}, {
    sequelize,
    tableName: "AgentModels",
    timestamps: false, // As per the migration (no created_at, updated_at)
    underscored: true,
    indexes: [
        { fields: ["agent_id"] },
        { fields: ["ai_model_id"] },
    ],
});
//# sourceMappingURL=agentModel.js.map